#!/usr/bin/make -f

%:
	dh $@

override_dh_install:
	--size standard compact --theme all --tweaks rimless normal all -dest $(CURDIR)/debian/colloid-gtk-theme/usr/share/themes
	find $(CURDIR)/debian/colloid-gtk-theme/usr/share/themes -name COPYING -delete
